{
  "name": "@meshsmo/sensors-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "react-router build",
    "prebuild": "node scripts/generate-registry.mjs",
    "build:dotnet": "npm run typecheck && npm run build",
    "dev": "react-router dev",
    "lint": "tsc --noEmit",
    "test": "node --test",
    "typecheck": "react-router typegen && tsc --noEmit"
  },
  "dependencies": {
    "isbot": "^5",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router": "^8",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@react-router/dev": "^8",
    "@types/node": "^22",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3",
    "vite": "^8.0.3"
  }
}
